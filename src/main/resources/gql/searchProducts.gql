query searchProducts($query: String!, $first: Int!,$sortKey: ProductSortKeys!) {
    products(
        query: $query
        first: $first
        sortKey: $sortKey
    ) {
        edges {
            cursor
            node {
            $product.fragment.gql
        }
    }
}
}
